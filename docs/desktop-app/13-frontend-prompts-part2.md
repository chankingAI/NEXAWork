# NexaWork 前端开发提示词 — Part 2（N12-N23）

> Phase 3 + Phase 4：专家系统 + 技能市场 + 自动化 + 项目 + 设置

---

## Prompt N12 — 专家列表页

```
任务：实现 AI 专家市场列表页（对应 WorkBuddy 截图8）。

UI 参考（WorkBuddy 截图8）：
- 顶部 Tab：专家 / 技能 / 连接器
- 搜索栏（右侧"我的专家"按钮）
- 精选场景卡片（4个大图卡片横向滚动）：内容创作/投资分析/法律咨询/小微企业
- 分类标签行：全部/OPC/腾讯专区/产品设计/技术工程/金融投资/...
- 专家网格（3列）：每项含图标/名称/描述/标签

要求：
1. ExpertListPage 组件：
   - 顶部 Tab 栏：专家(选中) / 技能 / 连接器
   - 搜索框 + "我的专家" 按钮
   - 精选场景区：
     - 4个大图卡片（250x160px）
     - 每卡片包含标题 + 2-3个专家/团队名称
     - 横向滚动（可拖拽）
   - 分类标签行：
     - 可滚动标签（全部/最热/最新/分类...）
     - 选中态：下划线
   - 专家网格（3列）：
     - ExpertCard 组件：头像(40px) + 名称 + 角色描述 + 短介绍 + 标签列表
2. 数据结构：
   - Expert: { id, name, role, avatar, description, tags[], category, usageCount }
   - ExpertTeam: { id, name, members: Expert[], description, scenarios[] }
3. 分页加载：滚动到底自动加载更多

集成约束：
- 专家配置从 src/services/ 下的 Agent 系统加载
- 每个专家对应一个 system prompt 配置
- 支持用户自创专家（存储到本地）

验收标准：
- 专家列表正确显示
- 搜索过滤实时生效
- 分类切换正确
- 精选场景卡片可滚动
```

---

## Prompt N13 — 专家团队详情

```
任务：实现专家团队详情弹窗（对应 WorkBuddy 截图9）。

UI 参考（WorkBuddy 截图9）：
- 模态弹窗（居中，圆角，阴影）
- 顶部：团队头像 + 名称 + 创建者 + 使用次数
- "能力介绍" 段落描述
- "擅长领域" 标签列表
- "团队成员" 列表：
  - 每项：角色图标 + 角色名 + 负责人名 + 标签（主理人/成员）
- 底部按钮："召唤 [团队名]"（黑色全宽按钮）

要求：
1. ExpertTeamDialog 组件：
   - 触发：点击专家列表中的团队卡片
   - 头部区域：
     - 团队头像（64px 圆形）
     - 团队名称（18px 粗体）
     - 创建者信息 + 使用次数
     - 关闭按钮（X，右上角）
   - 能力介绍：多行描述文本
   - 擅长领域：标签列表（圆角灰底小标签）
   - 团队成员列表：
     - 每项：头像(32px) + 角色名 + 负责人 + 角色标签
     - 区分"主理人"（蓝色标签）和"成员"
   - 底部操作栏：
     - "召唤 [团队名]" 按钮（黑底白字，full-width，圆角）
2. 召唤逻辑：
   - 点击"召唤" → 切换当前会话的 Agent 为该团队
   - 团队模式 = 主理人协调 + 成员各司其职

集成约束：
- 使用 Radix UI Dialog 组件
- 召唤后对话开头显示团队介绍
- 复用 Swarm Agent 系统的多角色协作

验收标准：
- 弹窗显示正确
- 成员列表完整
- 召唤后 AI 以团队身份回复
- 关闭弹窗回到列表页
```

---

## Prompt N14 — 专家选择器（输入栏集成）

```
任务：实现底部输入栏的专家快速选择器（对应 WorkBuddy 截图10）。

UI 参考（WorkBuddy 截图10）：
- 输入栏左下方显示当前专家（头像+名称）
- 点击展开下拉：
  - "最近召唤专家" 标题
  - 最近3个专家（头像+名称+选中勾号）
  - "更多" 按钮 → 打开专家列表页
  - "召唤其它专家" 链接
- 底部工具栏显示：专家名 + 自动 + 技能 + 默认权限

要求：
1. ExpertSelector 组件（输入栏底部集成）：
   - 当前专家显示：头像(20px) + 名称 + 下箭头
   - 弹出面板：
     - "最近召唤专家" 标题
     - 最近使用的3个专家（头像 + 名称 + 选中态勾号）
     - 分隔线
     - "更多" 按钮 + "召唤其它专家" 链接
   - 选择专家后：
     - 更新底部工具栏显示
     - 下次对话使用该专家 system prompt
2. 工具栏集成（输入框下方一行）：
   - [专家名] ▾ | [自动] ▾ | [技能] ▾ | [权限] ▾
   - 每项可独立配置

验收标准：
- 快速切换专家（2步操作）
- 最近专家记录正确
- 切换后 AI 行为变化
- 工具栏各项独立可配
```

---

## Prompt N15 — 技能搜索面板

```
任务：实现技能搜索和浏览面板（对应 WorkBuddy 截图4）。

UI 参考（WorkBuddy 截图4）：
- 搜索框（顶部）
- 技能列表：
  - 每项：图标(彩色圆形) + 名称 + 描述
  - 技能创建指南 / self-improving-agent / Brave Search CLI / ...
- 底部 "导入技能" 按钮

要求：
1. SkillSearchPanel 组件：
   - 搜索输入框（顶部，实时过滤）
   - 技能列表：
     - SkillItem 组件：图标(36px 圆形彩色背景) + 名称(14px 粗体) + 描述(12px 灰色)
     - 点击 → 启用/配置该技能
   - 分类：
     - 内置技能（预装）
     - 已安装技能
     - 可用技能（市场）
   - 底部操作：
     - "导入技能" 按钮（从文件/URL/仓库导入）
2. 技能详情面板（点击技能后）：
   - 名称 + 完整描述
   - 权限需求列表
   - 启用/禁用开关
   - 配置参数（如有）

集成约束：
- 对接 bundledSkills.ts 的内置技能
- 对接 .claude/skills/ 目录的自定义技能
- 对接 recorder 生成的 Skill

验收标准：
- 搜索实时过滤
- 技能启用/禁用正确
- 导入外部技能成功
- 技能列表与后端同步
```

---

## Prompt N16 — 技能管理（安装/导入/启用）

```
任务：实现技能的完整生命周期管理。

要求：
1. SkillManagementPage 组件（"更多"菜单下的子页面）：
   - Tab: 已安装 / 市场 / 我创建的
   - 已安装 Tab：
     - 技能卡片网格
     - 每卡片：图标+名称+描述+启用开关+删除按钮
     - 技能自动更新开关（全局设置）
   - 市场 Tab：
     - 分类浏览（开发/办公/自动化/数据）
     - 一键安装按钮
   - 我创建的 Tab：
     - 创建新技能按钮
     - 从录制生成的技能
     - 手动编写的技能
2. 安装流程：
   - 选择技能 → 查看权限需求 → 确认安装 → 完成
3. 导入方式：
   - 从文件导入（SKILL.md）
   - 从 URL 导入
   - 从 Git 仓库导入
4. 创建技能向导：
   - 名称/描述/触发条件
   - 指令编辑（Markdown）
   - 测试运行

集成约束：
- 复用现有 Skill 加载系统
- 录制生成的技能自动出现在"我创建的"列表

验收标准：
- 技能安装/卸载/启用/禁用完整
- 市场浏览流畅
- 自创技能可编辑
- 技能自动更新工作
```

---

## Prompt N17 — 权限控制 UI

```
任务：实现权限选择和管理（对应 WorkBuddy 截图5）。

UI 参考（WorkBuddy 截图5）：
- 底部工具栏 "默认权限 ▾" 下拉
- 选项：默认权限(选中) / 完全访问权限(警告图标)

要求：
1. PermissionSelector 组件：
   - 下拉选项：
     - 默认权限（盾牌图标）— 沙箱内安全操作
     - 完全访问权限（警告三角图标）— 完整系统访问
   - 切换到"完全访问"时显示确认对话框
2. PermissionConfirmDialog 组件（工具执行时）：
   - 当工具需要权限时弹出
   - 显示：工具名称 + 操作描述 + 影响范围
   - 按钮：允许一次 / 本次会话始终允许 / 拒绝
3. 权限日志：
   - 记录所有权限请求和结果
   - 可在安全中心查看

集成约束：
- 对接 src/types/permissions.ts 权限类型
- 对接 src/components/permissions/ 现有逻辑
- sandbox-runtime 沙箱策略集成

验收标准：
- 默认权限下危险操作被拦截
- 完全访问权限需二次确认
- 权限日志完整记录
```

---

## Prompt N18 — 自动化列表面板

```
任务：实现自动化任务管理面板（对应 WorkBuddy 截图11）。

UI 参考（WorkBuddy 截图11）：
- 顶部：标题"自动化" + 描述 + "添加"/"从模板添加" 按钮
- 已安排区域：
  - 每行：状态点(绿/黄) + 任务名 + 空间名 + 频率 + 生效期 + 距下次时间
- 已完成区域：
  - 每行：状态点(红) + 任务名 + 空间名 + 标签(失败/成功) + 时间 + 删除按钮

要求：
1. AutomationPanel 组件：
   - 头部：
     - 标题 "自动化"
     - 描述文字
     - "+ 添加" 按钮 + "从模板添加" 按钮
   - 已安排列表：
     - AutomationItem 组件：
       - 状态指示点（绿色=运行中，黄色=等待）
       - 任务名称（粗体）
       - 关联空间/项目
       - 频率描述（"每天 08:00"）
       - 生效日期范围
       - 距下次执行时间（"4小时后开始"）
   - 已完成列表：
     - 同样结构 + 执行结果标签（成功绿/失败红）
     - 删除按钮（X）
2. 数据结构：
   - Automation: { id, name, workspace, prompt, schedule, validFrom, validTo, status, lastRun }
3. 实时更新：自动化执行完成后列表自动刷新

集成约束：
- 对接 CronCreate/CronDelete/CronList 工具
- 执行记录存储到 SQLite
- 支持暂停/恢复自动化

验收标准：
- 已安排/已完成列表正确
- 倒计时实时更新
- 失败任务显示红色标签
- 可删除已完成记录
```

---

## Prompt N19 — 添加自动化表单

```
任务：实现添加自动化任务的表单弹窗（对应 WorkBuddy 截图12）。

UI 参考（WorkBuddy 截图12）：
- 弹窗标题："添加自动化任务"
- 表单字段：
  - 名称（文本输入）
  - 工作空间（可选，+ 添加）
  - 提示词（多行文本区域）
    - 底部工具：Auto ▾ / 技能 ▾ / 召唤专家 ▾ / 完全访问权限
  - 连接器（下拉选择）
  - 执行频率：周期/按间隔/单次
    - 周期：每天/每周 + 时间选择
  - 生效日期区间
- 底部按钮：取消 / 添加（黑色）

要求：
1. AddAutomationDialog 组件：
   - 名称输入（必填）
   - 工作空间选择（可选，下拉 + 新建）
   - 提示词编辑器：
     - 多行文本区域（200px 高）
     - 底部工具栏：模型选择 / 技能 / 专家 / 权限
   - 连接器选择（MCP Server 列表）
   - 执行频率：
     - Tab 切换：周期 / 按间隔 / 单次
     - 周期：天/周选择 + 时间选择器
     - 按间隔：数字 + 单位（分钟/小时/天）
     - 单次：日期时间选择
   - 生效日期区间（起止日期 picker）
   - 按钮：取消（灰色描边）/ 添加（黑色实心）
2. 表单验证：
   - 名称必填
   - 提示词必填
   - 频率必选
3. 提交逻辑：
   - 创建 Cron 任务（调用 CronCreateTool）
   - 保存配置到 SQLite
   - 关闭弹窗 + 刷新列表

验收标准：
- 表单填写完整可提交
- 验证失败显示错误提示
- 创建成功后出现在已安排列表
- 周期/间隔/单次模式切换正确
```

---

## Prompt N20 — 项目管理页

```
任务：实现项目管理页面（对应 WorkBuddy 截图7）。

UI 参考（WorkBuddy 截图7）：
- 标题："项目 — 多人协同，打造超级团队"
- "+ 新建项目" 按钮
- "我的项目" 区域：已创建的项目列表
- "从模版创建" 区域：
  - 6个模板卡片（图标+名称+描述）：
    - 产品需求全流程、市场调研与竞品分析、团队知识库
    - 项目交付、Bug跟踪/测试验收

要求：
1. ProjectPage 组件：
   - 头部：标题 + 描述 + 插图 + 搜索框
   - "+ 新建项目" 按钮
   - "我的项目" 区域：
     - 项目卡片：图标 + 名称 + 描述 + 创建时间
     - 右键菜单：打开/重命名/删除
   - "从模版创建" 区域：
     - 模板网格（3列）
     - 每模板：图标(48px) + 名称(粗体) + 描述(灰色)
     - 点击模板 → 创建带预设的新项目
2. 项目数据结构：
   - Project: { id, name, description, template, path, createdAt }
   - 关联本地 Git 仓库路径
3. 新建项目向导：
   - 步骤1：选择模板或空白
   - 步骤2：名称/描述
   - 步骤3：选择本地目录
   - 步骤4：初始化（git init 等）

验收标准：
- 项目列表正确显示
- 从模板创建项目成功
- 项目与本地目录关联
- 可在项目内开启对话
```

---

## Prompt N21 — 系统设置面板

```
任务：实现系统设置面板（对应 WorkBuddy 截图13）。

UI 参考（WorkBuddy 截图13）：
- 左侧设置导航：账户管理/系统设置/智能体设置/记忆/模型/助理设置/个性化/数据管理/安全中心/帮助与反馈
- 右侧设置内容：
  - 显示语言：中文(简体) 下拉
  - 字体大小：滑块（小-默认-大）
  - 阅读模式：开关（对话界面纯文本显示）
  - 发送消息：Enter 下拉
  - 技能自动更新：开关
  - 非高风险技能自动安装：开关
  - 锁屏远程：开关
  - 默认工作空间存储确认：开关

要求：
1. SettingsPage 组件：
   - 左侧导航（10项）
   - 右侧内容区（根据选中项切换）
2. 系统设置 Tab：
   - 显示语言：下拉选择（中文简体/英文/日文）
   - 字体大小：滑块（12-20px，步进1）
   - 阅读模式：开关（绿色）
   - 发送消息键：下拉（Enter / Ctrl+Enter）
   - 技能自动更新：开关
   - 非高风险技能自动安装：开关
   - 锁屏远程：开关
   - 默认存储路径确认：开关
3. 设置持久化：
   - 写入 settings.json
   - 实时生效（无需重启）

集成约束：
- 使用 Radix UI 组件（Switch/Slider/Select）
- 设置变更通过 IPC 同步到 Main Process
- 语言切换支持 i18n

验收标准：
- 所有设置项可修改
- 修改实时生效
- 重启后设置保持
- 语言切换整个界面更新
```

---

## Prompt N22 — 智能体/助理/记忆设置

```
任务：实现 AI 行为相关设置。

要求：
1. 智能体设置 Tab：
   - System Prompt 编辑器（多行文本，Markdown 支持）
   - 温度设置（0-1 滑块）
   - 最大 Token 设置（滑块/数字输入）
   - 启用的工具列表（多选复选框）
2. 助理设置 Tab：
   - 助理名称（自定义）
   - 助理头像（上传/预设选择）
   - 问候语设置
   - 回复风格：专业/友好/简洁
3. 记忆设置 Tab：
   - 记忆开关（总开关）
   - 记忆内容查看（列表，可删除单条）
   - 清空所有记忆按钮（红色，需确认）
   - 操作记忆设置（频率/保留时长）
4. 模型设置 Tab：
   - 默认模型选择
   - API Key 配置（各 provider）
   - 自定义端点配置
   - 模型测试（发送测试消息验证连接）

集成约束：
- 记忆对接 operationMemory 系统
- API Key 加密存储（Electron safeStorage）
- 模型配置对接 7 个 API Provider

验收标准：
- System Prompt 修改即时影响 AI 行为
- 记忆可查看和清除
- API Key 配置后模型可用
- 测试连接返回成功/失败
```

---

## Prompt N23 — 数据管理

```
任务：实现数据导入/导出/清理功能。

要求：
1. DataManagementPage 组件：
   - 数据统计：会话数/消息数/技能数/占用空间
   - 导出数据：
     - 选择范围（全部/日期范围/指定会话）
     - 格式选择（JSON/Markdown/PDF）
     - 导出按钮 → 保存到文件
   - 导入数据：
     - 从文件导入会话/技能
     - 格式验证 + 冲突处理
   - 清理数据：
     - 清空所有会话（需二次确认）
     - 清空缓存文件
     - 重置所有设置
   - 备份/恢复：
     - 一键备份到指定目录
     - 从备份恢复

验收标准：
- 导出数据格式正确可读
- 导入后数据完整显示
- 清理操作不可逆（有确认）
- 备份/恢复完整无损
```
